{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute baby-diary-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute baby-diary-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.10",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250507.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.16.0"
  }
}
